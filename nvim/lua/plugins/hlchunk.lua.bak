return {
  "shellRaining/hlchunk.nvim",
  config = function()
    require("hlchunk").setup({
      -- style = { "#abcabc", "#cdefef" },
      --       blank = {
      --   enable = false,
      -- }
    })
  end
}
